const finalParticipants = ["Taylor","Donal","Don","Natasha","Bobby"];

const announcements = finalParticipants.map((membar) => {
    return membar + "joined the contest.";
});

console.log(announcements);